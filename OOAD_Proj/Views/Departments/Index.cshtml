@model IEnumerable<OOAD_Proj.Models.Department>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.D_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.D_Building)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.D_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.D_Building)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.D_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.D_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.D_id })
        </td>
    </tr>
}

</table>
