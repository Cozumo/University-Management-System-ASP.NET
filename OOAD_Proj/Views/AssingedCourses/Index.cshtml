@model IEnumerable<OOAD_Proj.Models.AssingedCourse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Acourse_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Course1.course_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student1.S_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Teacher1.T_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Acourse_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Course1.course_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student1.S_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Teacher1.T_id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ACourses_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.ACourses_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ACourses_id })
        </td>
    </tr>
}

</table>
